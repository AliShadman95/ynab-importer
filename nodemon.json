{
  "restartable": "rs",
  "ignore": [".git", "node_modules/**/*"],
  "verbose": true,

  "ext": "js,json",
  "events": {
    "start": "echo [nodemon] Starting with NODE_ENV=${NODE_ENV} and ENV_TYPE=${ENV_TYPE}"
  }
}
